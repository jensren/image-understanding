Run a2.py with numpy, scipy, cv2, and matplotlib installed. The images folder should be in the same directory as a2.py.
If desired, toggle which parts are tested in the if __name__ == "__main__": portion of the code. The code will take a
couple minutes to run.