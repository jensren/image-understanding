Run the program with opencv, numpy, scipy, and matplotlib installed.

The images folder must be in the same folder as a1.py.

To terminate the program, close all the windows that are displaying images.