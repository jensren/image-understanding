Run a3.py with numpy, scipy, cv2, and matplotlib installed. The images folder, ransac.py, and sift_features.mat should
be in the same directory as a2.py. If desired, toggle which parts are tested in the if __name__ == "__main__": portion
of the code. The code will take a while to run.